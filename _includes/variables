{% comment %}
This object will allow the JavaScript to access the page variables
set in the YAML frontmatter, so that the dashboard be be populated on each page.
{% endcomment %}

<div class="visuallyhidden">
    <span class="temperature-value">{{ page.temperature }}</span>
    <span class="decade-value">{{ page.decade }}</span>
    <span class="conflict-value">{{ page.conflict }}</span>
    <span class="economy-value">{{ page.economy }}</span>
</div>
